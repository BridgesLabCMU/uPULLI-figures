# Paper-figure reproduction — pinned to the versions the published figures were produced with (Python 3.9).
#
# Rendering the panels from the bundled source-data tables (data/) needs the core set.
# Rebuilding those tables from the raw feature matrix (build/, requires the KiltHub wide table)
# additionally needs the "rebuild" set.
#
# The figures assume the 'Gillius ADF' font. figlib prints a one-time warning if it is missing, since
# matplotlib otherwise falls back silently and text spacing drifts from the published figures (the data
# and coordinates are unaffected). It is not bundled here: it is GPL-2+ with font exception, and this
# repository is MIT, so it is installed rather than redistributed.
#   Debian/Ubuntu:  apt install fonts-adf-gillius
#   macOS/Windows/other:  https://arkandis.tuxfamily.org/adffonts.html  (Gillius ADF, install the .otf)

# --- core: render the panels from data/ ---
numpy==2.0.2
pandas==2.3.1
matplotlib==3.9.4
scipy==1.13.1          # dendrogram tree drawing (Panel D)

# --- rebuild: regenerate the tables from the raw feature matrix (build/) ---
scikit-learn==1.6.1
umap-learn==0.5.9.post2
numba==0.60.0
pyarrow==21.0.0        # read the .parquet feature matrix / embeddings
